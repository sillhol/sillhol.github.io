# 🟦 Покрокова інструкція з встановлення та налаштування Git на Windows 10/11

## 1. Завантаження Git для Windows

1. Відкрийте браузер і перейдіть на сайт Git у розділ [завантаження](https://git-scm.com/install/windows).  
2. Завантажте інсталятор **Git for Windows/x64 Setup**.

---

## 2. Встановлення Git

Запустіть інсталятор і виконайте такі кроки майстра:

- **License Agreement** → натисніть *Next*  
- **Installation Path** → залиште шлях за замовчуванням  
- **Select Components** → залиште рекомендовані параметри  
- **Choosing the default editor** → оберіть Notepad або Visual Studio Code  
- **Adjusting your PATH environment** → оберіть **Git from the command line and also from 3rd‑party software**  
- **Choosing SSH executable** → *Use bundled OpenSSH*  
- **Choosing HTTPS transport backend** → *Use HTTPS*  
- **Configuring line ending conversions** → оберіть **Checkout Windows‑style, commit Unix‑style**  
- **Configuring the terminal emulator** → *Use MinTTY*  
- **Additional options** → увімкніть *Enable file system caching*  
- Натисніть **Install**, дочекайтеся завершення → **Finish**

---

### Перевірка встановлення

Відкрийте **Git Bash** або **PowerShell** і введіть:

```bash
git --version
```

Якщо бачите номер версії — Git встановлено.

---

### Базова конфігурація Git

Встановлення імені та email

```bash
git config --global user.name "Ваше ім'я"
git config --global user.email "you@example.com"
```

Перевірка:

```bash
git config --global --list
```

---

## 3. Налаштування SSH‑ключів у Windows

Використовуйте PowerShell або Git Bash з Windows-шляхами.

### 3.1. Перевірка наявних ключів

**PowerShell:**

```powershell
Get-ChildItem $env:USERPROFILE\.ssh
```

**Git Bash:**

```bash
ls -al ~/.ssh
```

> У Git Bash `~` автоматично посилається на `C:\Users\ВашКористувач`.

---

### 3.2. Створення нового SSH-ключа

```bash
ssh-keygen -t ed25519 -C "you@example.com"
```

Натисніть **Enter**, щоб прийняти шлях за замовчуванням.

---

### 3.3. Запуск ssh-agent і додавання ключа

```bash
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519
```

---

### 3.4. Отримання публічного ключа

```bash
cat ~/.ssh/id_ed25519.pub
```

Скопіюйте ключ і додайте його у GitHub (або у GitLab/Bitbucket, якщо колись будемо їх використовувати) → **SSH Keys**.

---

### 3.5. Перевірка підключення

```bash
ssh -T git@github.com
```

---
## 4. Робота із репозиторієм
### 4.1 Клонування репозиторію

HTTPS:

```bash
git clone https://github.com/sillhol/hybridsfd.git
```


---

### 4.2 Створення локального репозиторію (для Вас не потрібно)

```bash
mkdir my-project
cd my-project
git init
echo "Мій перший репозиторій" > README.md
git add README.md
git commit -m "Перший коміт"
```

---

### 4.3. Прив’язка до віддаленого репозиторію (для Вас не потрібно)

```bash
git remote add origin git@github.com:username/my-project.git
git push -u origin main
```

---

## 5. Git LFS (Large File Storage) — робота з великими (бінарними) файлами

Git LFS потрібен, якщо у проєкті є:

- відео (`*.mp4`, `*.mov`)
- аудіо (`*.wav`, `*.mp3`)
- великі архіви (`*.zip`, `*.7z`)
- 3D-моделі, ML-моделі, PSD тощо (`*.pdf`, `*.doc`, `*.docx`)

### 5.1. Встановлення Git LFS

У Git Bash:

```bash
git lfs install
```

---

### 5.2. Додавання типів файлів у LFS
(вже зроблено. Робити тільки якщо в файлі `.gitattributes` ще немає відповідного розширення файлу)

```bash
git lfs track "*.mp4"
git lfs track "*.zip"
git lfs track "*.psd"
```

У результаті виконання команди Git створить або оновить `.gitattributes`.

Не забудьте додати `.gitattributes` до індексу та закомітити:

```bash
git add .gitattributes
git commit -m "Налаштовано Git LFS"
```

---

## 6. Додавання файлів у репозиторій

Процес додавання файлів у Git однаковий як для звичайних файлів, так і для тих, що відстежуються Git LFS.  
Якщо тип файлу вже внесений у `.gitattributes` через `git lfs track`, Git автоматично обробить його через LFS.

### Приклад додавання файлу:

```bash
git add video.mp4
git commit -m "Додано відео"
git push
```

Git сам визначить, чи це LFS-файл, чи звичайний, і виконає відповідні дії.

---
